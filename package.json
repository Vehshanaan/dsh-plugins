{
  "name": "dsh-plugins",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "description": "Claude Code style command and safety plugins for DeepSeek Harness (dsh)",
  "scripts": {
    "build": "corepack pnpm -r run build",
    "test": "corepack pnpm -r run test",
    "typecheck": "corepack pnpm -r run typecheck"
  }
}